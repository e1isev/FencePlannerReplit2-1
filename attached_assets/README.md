# Attached assets

Place the `Fence Planner SKU List.xlsx` workbook in this directory before running the
`extract:residential-pricing` script. The workbook is intentionally not checked into
version control due to binary file restrictions.
